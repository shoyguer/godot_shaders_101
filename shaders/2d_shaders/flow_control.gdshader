shader_type canvas_item;

void vertex() {
	// If & Else
	int my_int = 9;
	if(my_int == 9) {

	}
	else if(my_int != 9) {

	}
	else {

	}

	// While loop
	int i = 0;
	while(i < 10) {
		i += 1;
	}

	// Do while loop
	do {

	} while(false);

	// For Loop
	for(int j = 0; j < 5; j++) {

	}

	// Switch statement
	int my_switch = 1;
	switch(my_switch) {
		case 1:
			break;

		case 2:
			break;

		default:
			break;
	}
}

void fragment() {
	// Called for every pixel the material is visible on.
}
